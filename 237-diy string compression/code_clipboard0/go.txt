package main
import (
  "fmt"
  "strconv"
  "encoding/json"
  "reflect"
)

func StringCompression(chars *[]byte) int{
  // Write your code here
  // To access chars variable you can use steric
  return (len(*chars) - 1) // Kindly change the return value as per your needs.
}